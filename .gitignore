# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# Outputs: the logs and metrics are versioned on purpose, the weights cannot be
# (423 MB is past GitHub's file limit), so only the model directories are ignored
outputs/models/

# MLflow: kept in git so the loss curves survive a deleted pod, as thibault-logging asks
# mlruns/

# Reference PDFs (heavy, see papers/_INDEX.md for the arXiv links)
papers/**/*.pdf

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Testing / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Logs
*.log
.cache/
results.tar.gz
